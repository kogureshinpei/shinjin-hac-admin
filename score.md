# 採点基準

## 擬似クラス・擬似要素の活用

### 1. hover: 擬似クラス（20点）

| 評価項目 | 配点 | 確認ポイント |
|---|---|---|
| ナビゲーションリンクに `hover:` が設定されている | 5点 | マウスオーバーで色・下線などの変化があること |
| カード要素に `hover:` エフェクトが設定されている | 5点 | 活動紹介・メンバーカードなど複数のカードに適用されていること |
| 画像・アイコンに `hover:` が設定されている | 5点 | プロフィール写真など画像要素にインタラクションがあること |
| フッターリンクに `hover:` が設定されている | 5点 | ソーシャルリンク・メールリンクにホバーエフェクトがあること |

### 2. transition / animation（15点）

| 評価項目 | 配点 | 確認ポイント |
|---|---|---|
| `transition` プロパティで変化が滑らか | 5点 | 瞬時に切り替わらず、適切な duration が設定されていること |
| `transform`（translate・scale）による動きがある | 5点 | 浮き上がりや拡大など、位置・サイズの変化にアニメーションがあること |
| easing（ease / ease-in-out 等）が適切 | 5点 | 動きに緩急があり、不自然なリニア動作でないこと |

### 3. ::before / ::after 擬似要素（25点）

| 評価項目 | 配点 | 確認ポイント |
|---|---|---|
| `::after` が装飾目的で使われている | 10点 | 見出し下のアクセントバーなど、`content: ''` を使った装飾要素が存在すること |
| `::before` が装飾目的で使われている | 10点 | リンク下線など、`content: ''` を使った装飾要素が存在すること |
| 擬似要素がホバー時に変化する | 5点 | `:hover::after` / `:hover::before` などでインタラクティブな変化があること |

### 4. レイアウトへの影響（20点）

| 評価項目 | 配点 | 確認ポイント |
|---|---|---|
| ホバー前後でレイアウトが崩れていない | 10点 | 要素がずれたり、スクロールバーが出たりしないこと |
| `transform` を使って周囲の要素に影響を与えていない | 10点 | `margin` / `padding` ではなく `transform` で動かし、document flow を変えていないこと |

### 5. コードの品質・整合性（20点）

| 評価項目 | 配点 | 確認ポイント |
|---|---|---|
| クラス名が意味のある命名になっている | 5点 | `.nav-link`・`.card`・`.section-heading` など役割がわかる命名 |
| Tailwind の hover: ユーティリティと CSS の擬似要素が混在しても競合していない | 5点 | スタイルの上書き・競合が起きていないこと |
| 擬似要素に `content` プロパティが明示されている | 5点 | `content: ''` の記述漏れがないこと |
| 複数箇所に一貫してエフェクトが適用されている | 5点 | 一部の要素だけでなく、同種の要素には統一されたエフェクトがあること |

---

## 合計：100点満点

| カテゴリ | 配点 |
|---|---|
| hover: 擬似クラス | 20点 |
| transition / animation | 15点 |
| ::before / ::after 擬似要素 | 25点 |
| レイアウトへの影響 | 20点 |
| コードの品質・整合性 | 20点 |
| **合計** | **100点** |
